**Commit workflow**

- After any change, run `git add . && git commit -m "<type>(scope?): <short description>"` following the Conventional Commits spec (e.g., `feat(auth): add login endpoint`).
- Push with `git push`.
